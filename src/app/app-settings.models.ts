export interface SidebarSettings {
    whatIfAnalysisVisible: boolean,
}

export interface OrgSetting {
    companyName: string,
    companyLink: string,
    title: string,
    favicon: string,
    version:string
}

export interface IDownloadSetting{
    rows: number,
    minRows: number,
    maxRows: number
}