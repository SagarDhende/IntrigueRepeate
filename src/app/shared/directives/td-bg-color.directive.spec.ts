import { TdBgColorDirective } from './td-bg-color.directive';

describe('TdBgColorDirective', () => {
  it('should create an instance', () => {
    const directive = new TdBgColorDirective();
    expect(directive).toBeTruthy();
  });
});
