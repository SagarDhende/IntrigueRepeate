import { TdTextDirective } from './td-text.directive';

describe('TdTextDirective', () => {
  it('should create an instance', () => {
    const directive = new TdTextDirective();
    expect(directive).toBeTruthy();
  });
});
